; better-auth declares optional peer deps for many frameworks (e.g. @sveltejs/kit)
; that conflict during resolution. We don't use those frameworks, so relax peer
; resolution — needed for both `npm install` and CI's `npm ci` to succeed.
legacy-peer-deps=true
